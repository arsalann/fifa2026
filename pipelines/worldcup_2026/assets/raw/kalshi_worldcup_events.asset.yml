name: raw.kalshi_worldcup_events
type: ingestr
connection: motherduck-fifa
description: Open Kalshi World Soccer Cup events with nested markets for app insights.
parameters:
  source_connection: kalshi-worldcup-events
  source_table: events
  destination: motherduck
  version: v1
  incremental_strategy: replace
  schema_naming: direct

columns:
  - name: event_ticker
    type: varchar
    primary_key: true
